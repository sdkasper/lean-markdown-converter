[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --strict-markers
markers =
    security: Tests for security fixes (symlink traversal, path filtering)
    integration: Tests that run actual MarkItDown conversions
    slow: Tests that take more than 1 second
